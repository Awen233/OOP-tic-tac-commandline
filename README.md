# OOP-tic-tac-commandline
to compile in commandline, enter the project directory,
then type:

javac $(find . -name "*.java")

cd src

java -cp . course.oop.main.TTTDriver


then the project starts running!

GO GATOR!!
